<%= form_for(@remark) do |f| %>
<%= hidden_field_tag 'back_to' %>
<%= f.hidden_field :lock_version %>
  <% if @remark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@remark.errors.count, "error") %> prohibited this remark from being saved:</h2>

      <ul>
      <% @remark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table class="show_table">

  <tr>
    <th><%= f.label :id, getLongName('remarks', 'id') %></th>
    <td><%= f.text_field :id %></td>
  </tr>

  <tr>
    <th><%= f.label :owner_id, getLongName('remarks', 'owner_id') %></th>
    <td><%= f.number_field :owner_id %></td>
  </tr>

  <tr>
    <th><%= f.label :remark_key, getLongName('remarks', 'remark_key') %></th>
    <td><%= f.text_field :remark_key %></td>
  </tr>

  <tr>
    <th><%= f.label :remark_target_id, getLongName('remarks', 'remark_target_id') %></th>
    <td><%= f.number_field :remark_target_id %></td>
  </tr>

  <tr>
    <th><%= f.label :remark_content, getLongName('remarks', 'remark_content') %></th>
    <td><%= f.text_field :remark_content %></td>
  </tr>

  <tr>
    <th><%= f.label :rating, getLongName('remarks', 'rating') %></th>
    <td><%= f.number_field :rating %></td>
  </tr>

</table>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
